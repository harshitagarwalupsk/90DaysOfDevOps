
### hello.sh script
Created my first hello.sh cript and try to run it after removing shibang it run fine without any error and out was as expected.

### variable.sh script
Successfully created variables file where I mentioned two variables:

1) Name="Harshit" and 2) Role="Engineer"

Then I echo it using $Name and $Role and it provided the output as expected.

Note: Quotes changed from double to single but the output is same, there is no change or no error in the script.

### gret.sh script
Successfully run below command to take input

read -p "Enter:" Variable

### check_number.sh script
Many syntex error while writing the code but the major issue was space inside [] that I found using the google.

#### if [ $NUM -lt '0' ]; then
####       echo "Number is Negative"
#### elif [ $NUM -gt '0' ]; then
####       echo "Number is Positive"
#### else
####       echo "Number is Zero"
#### fi

### file_check.sh
created and validated successfully

### server_check.sh
Done! without any google help :)
